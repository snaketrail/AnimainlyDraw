# dependencies
node_modules/

# build output
dist/
dist-ssr/
*.tsbuildinfo

# logs
*.log
npm-debug.log*

# editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# env and secrets
# API keys belong in the app's own settings (stored in the browser), never in
# a file here — this repo publishes to GitHub Pages, so anything committed is
# public permanently.
.env
.env.local
.env.*.local
*.key
secrets.json
**/secrets/
